<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.4//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gzhtdq.daas.api.mapper.ds1.DaasUserMapper1">
    <select id="list" resultType="DaasUser1">
		SELECT id, username, password, account_expired accountExpired, account_locked accountLocked, credentials_expired credentialsExpired, enabled FROM daas_user
	</select>
</mapper>

